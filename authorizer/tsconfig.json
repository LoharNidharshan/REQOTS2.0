{
    "compilerOptions": {
        "target": "ES2020", // Choose the appropriate target for your Node.js runtime
        "module": "CommonJS", // AWS Lambda expects CommonJS modules
        "outDir": "./dist", // Specify the output folder for compiled JS files
        "strict": true,
        "esModuleInterop": true
    },
    "include": [
        "authorizer.ts"
    ], // Ensure it includes the necessary files
    "exclude": [
        "node_modules"
    ]
}